/** Content-slot skeleton while the study page RSC streams in. Shell is already painted. */
export default function StudyLoading() {
    return <div className="flex-1 bg-[var(--base-100,#f8f8f7)]" />;
}
