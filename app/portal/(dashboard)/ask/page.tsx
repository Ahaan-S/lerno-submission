import DashboardContentWrapper from "@/components/dashboard/DashboardContentWrapper";

/**
 * Ask mode home — auth/profile/shell provided by the (dashboard) layout.
 * The Ask tab is activated automatically inside DashboardContent via usePathname().
 */
export default function AskPage() {
    return <DashboardContentWrapper />;
}
