/** Shell is already painted by the (dashboard) layout — this covers only the content slot. */
export default function SubjectLoading() {
    return <div className="flex-1 bg-[var(--base-100,#f8f8f7)]" />;
}
