/** Content-slot skeleton while the learn page RSC streams in. Shell is already painted. */
export default function LearnLoading() {
    return <div className="flex-1 bg-[var(--base-100,#f8f8f7)]" />;
}
